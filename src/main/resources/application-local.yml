

logging.file: logs/application.log
logging:
  level:
      org:
        springframework:
          web: DEBUG
        hibernate: ERROR
spring:
  datasource:
    url: jdbc:h2:file:./target/court
  h2:
    console:
      enabled: true
      path: /h2-console